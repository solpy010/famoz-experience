# Claude 인수인계 — FAMOZ 홈페이지 정보구조·체험 재설계

기준일: 2026-09-03  
작업 브랜치: `fix/hero-canvas-clear`  
상태: **구현 전 검토 확정안**  
우선순위: 시각 효과 추가보다 IA·콘텐츠 전달·인터랙션 문법 정상화가 먼저

> 이 문서는 기존 `CLAUDE_NEXT.md`보다 우선한다. 기존 문서의 OZ 구조 확장 지시는
> 잠정 중단한다. 현재 공유 WebGL·리소스 슬롯·실적 데이터는 보존하되, 화면 순서와
> 체류 길이, 내비게이션, 콘텐츠 위계는 이 문서를 기준으로 다시 설계한다.

---

## 0. 이번 작업의 목적

현재 사이트는 기술적으로는 하나의 파티클 공간과 여러 장면을 갖고 있으나,
첫 방문자에게는 다음처럼 읽힌다.

- 홈페이지보다 긴 프레젠테이션 또는 스크롤형 회사소개서에 가깝다.
- Hero, Portal, 캐릭터 장면, 선언형 문장이 연속되어 “문을 열었는데 또 문”처럼 느껴진다.
- 회사가 무엇을 만들고 어떤 결과를 냈는지 확인하기 전에 긴 도입을 통과해야 한다.
- 배경·인물·바·선·전환 규칙이 장면마다 달라 사용자가 새 인터페이스를 반복 학습한다.
- 인터랙션이 회사 메시지를 증명하기보다 스크롤 기술 실험처럼 보인다.
- 모바일에서는 인물 크롭과 텍스트 위계가 무너진다.

이번 작업의 목표는 기존 경험 자산을 폐기하는 것이 아니다. **경험은 짧고 선명한
도입 및 핵심 증거를 강화하는 장치로 재배치하고, 홈페이지의 뼈대는 일반 사용자가
즉시 이해할 수 있는 콘텐츠 구조로 복구한다.**

---

## 1. 작업 전 반드시 읽을 문서

아래 순서를 지킨다.

1. `AUDIT.md`
2. `HANDOFF.md`
3. `.claude/skills/famoz-art-direction/SKILL.md`
4. `.claude/skills/webgl-fluid-field/SKILL.md`
5. `.claude/skills/performance-tier/SKILL.md`
6. `.claude/skills/visual-qa/SKILL.md`
7. `CLAUDE_IA_RESTRUCTURE.md` — 본 문서
8. `RESOURCE_REQUEST.md`
9. `content/experienceManifest.ts`

Next.js 코드를 변경하기 전에는 `AGENTS.md`의 지시에 따라 현재 설치된 Next.js 16
문서를 확인한다.

---

## 2. 현재 구조에서 확인된 직접 원인

### 2-1. 도입과 선언 구간이 너무 길다

현재 `app/page.tsx` 순서:

```text
Intro → Hero → OzPortalHub → MascotScene → WhatWeCreate
      → ValueScene → PublicValue → WorksFilm → EndingScene
```

코드상 긴 sticky 구간이 연속된다.

| 구간 | 현재 상태 | 문제 |
|---|---|---|
| `MascotScene` | 데스크톱 다중 장면, 모바일 CSS `540vh` | 캐릭터 소개가 핵심 정보 진입을 지연 |
| `WhatWeCreate` | `520vh` | 앞 캐릭터 장면과 역량 메시지가 중복 |
| `ValueScene` | `500vh` | 선언을 다시 긴 스크롤로 설명 |
| `WorksFilm` | 별도의 장시간 sticky 구조 | 실제 증거가 너무 늦게 등장 |

### 2-2. 정보보다 인터랙션 장치가 먼저 읽힌다

- `ChapterNavigation`은 존재하지만 일반적인 회사 사이트 메뉴가 아니라 챕터 레일에 가깝다.
- `OzPortalHub`의 네 경로는 세계관 설명에는 유효하지만 초기 정보 탐색에는 추가 관문이다.
- Works의 진행 바·점·라인은 프로젝트 내용보다 먼저 시선을 끌 수 있다.
- 장면마다 다른 진행 표시와 전환 규칙이 있어 예측 가능성이 낮다.

### 2-3. 선언과 증거가 분리되어 있다

- “관람객 중심”, “몰입”, “AI”, “기억”을 선언하지만 바로 옆에 프로젝트 증거가 없다.
- 방문자는 파모즈가 무엇을 했고 무엇을 만들어주는지 확인하기 위해 Works까지 기다려야 한다.
- 캐릭터·세계관은 브랜드 기억 장치가 되어야 하지만 현재는 서비스 이해보다 먼저 나온다.

### 2-4. 모바일은 별도 편집이 아니라 축소·크롭에 가깝다

- 긴 sticky 높이는 유지되는데 가로 구도는 세로 화면에 맞지 않는다.
- 얼굴·도구·제목이 한 화면에서 경쟁하고 일부 캐릭터는 머리나 표정이 잘릴 수 있다.
- 데스크톱의 공간적 여백이 모바일에서는 빈 체류 시간으로 바뀐다.

---

## 3. 반드시 보존할 것

아래는 이번 재구성에서 삭제하거나 처음부터 다시 만들지 않는다.

- `VisualSystemCanvas` 하나를 공유하는 WebGL 구조
- `PointerField`, `JourneyState`, `scrollBus`의 중앙 입력 구조
- 장면 끝까지 유지되는 파티클 공간 및 성능 개선 결과
- WebGL 부재·context lost·reduced-motion 폴백
- `content/experienceManifest.ts`의 챕터/자산 슬롯 계약
- `RESOURCE_REQUEST.md`의 영상·이미지 규격
- `WorksFilm`에 정리된 실제 프로젝트 데이터와 최적화된 이미지
- 회사 정보, 연락처, SEO/메타데이터, 접근성 기반
- 장면별 기존 컬러 방향: 오렌지는 오렌지, 시안·골드는 시안·골드, 바이올렛은 바이올렛

기존 컴포넌트를 한 번에 삭제하지 않는다. 새 구조에서 사용 여부가 검증된 뒤
중복 컴포넌트를 제거한다.

---

## 4. 중단하거나 축소할 것

### 즉시 중단

- OZ Portal을 또 하나의 전면 표지 화면으로 확장하는 작업
- 캐릭터마다 별도의 긴 도입 장면 또는 전환 영상을 추가하는 작업
- 장면별 새 Canvas·renderer·스크롤 리스너 추가
- 설명 문장만으로 100vh 이상을 점유하는 신규 섹션
- 바·라인·점·게이지 등 새로운 장식성 진행 UI 추가
- 정보구조가 확정되기 전 파티클, Bloom, 셰이더 미술 재조정

### 축소 또는 재배치

- `OzPortalHub`: 별도 관문에서 **한 화면 안의 Capability Map**으로 축소
- `MascotScene`: 네 캐릭터의 장시간 연속 소개에서 **역량별 증거 구간의 안내자**로 변경
- `WhatWeCreate`, `ValueScene`, `PublicValue`: 중복 문장을 통합하고 프로젝트 근거와 결합
- `ChapterNavigation`: 장면 번호 레일에서 일반적인 고정 메뉴 + 현재 위치 표시로 변경
- Intro: 첫 방문 기준 5–8초 이내 또는 즉시 건너뛰기 가능; 재방문 시 축약

---

## 5. 목표 IA

전체 경험은 데스크톱 기준 약 10–14개의 실질 화면 안에서 핵심 정보를 전달한다.
한 섹션이 여러 화면을 쓰더라도 새로운 정보나 증거가 매 화면 나타나야 한다.

| 순서 | 섹션 | 방문자가 즉시 알아야 할 것 | 경험 장치 |
|---:|---|---|---|
| 01 | Intro / Hero | 파모즈는 공간 경험을 설계·제작하는 회사 | 짧은 자동 진행 + 최소 포인터 반응 |
| 02 | Company Proof | 무엇을 하고, 누구를 위해, 어떤 결과를 냈는가 | 대표 프로젝트 1개를 즉시 노출 |
| 03 | Capability Map | Planning·Immersive·Interactive·AI 네 역량 | OZ/캐릭터를 한 화면 내 선택 가능한 지도화 |
| 04 | Capability + Evidence | 각 역량이 실제 프로젝트에서 어떻게 작동했는가 | 역량별 짧은 설명 + 이미지/결과/역할 |
| 05 | Selected Works | 실제 수행 실적과 범위 | 전체 화면 프로젝트, 명확한 탐색 |
| 06 | Process / Team / Contact | 함께 일하는 방식과 다음 연결 | 안정된 마지막 공간 + 직접 연락 동선 |

### 핵심 원칙

1. 첫 2개 화면 안에 회사 정의와 실제 프로젝트가 모두 보여야 한다.
2. 선언형 문장 뒤에는 같은 화면 또는 다음 화면에 반드시 증거가 따라온다.
3. 세계관은 정보구조를 대체하지 않고 정보 이해를 돕는다.
4. Works는 뒤늦게 처음 등장하는 증거가 아니라 앞에서 예고되고 여기서 상세화된다.
5. 메뉴를 통해 어느 핵심 섹션으로든 즉시 이동할 수 있어야 한다.

---

## 6. 콘텐츠 구조

### 6-1. Hero

유지할 메시지:

> 상상을 살아 있는 공간 경험으로 디자인합니다.

반드시 함께 보여줄 정보:

- `FAMOZ — 공간 경험 디자인 스튜디오`
- `기획 · 미디어 콘텐츠 · 인터랙션 · AI 프로덕션`
- `프로젝트 보기`와 `무엇을 만드는지` 두 개 이하의 명확한 진입점

Hero가 끝나기 전에 대표 프로젝트 이미지 또는 쇼릴 일부가 보여야 한다. Hero 뒤에
또 다른 전면 표지를 배치하지 않는다.

### 6-2. Company Proof

한 화면에서 다음 질문에 답한다.

- 파모즈는 무엇을 만드는가?
- 어떤 공간과 관람객을 다루는가?
- 기획부터 운영 중 어디까지 수행하는가?
- 이를 증명하는 대표 프로젝트는 무엇인가?

가능하면 대표 프로젝트 1개의 `문제 → 파모즈 역할 → 결과`를 짧게 배치한다.
확인되지 않은 수치 성과는 만들지 않는다.

### 6-3. Capability Map

기존 OZ Portal의 세계관은 유지하되 독립된 문이 아니라 네 역량을 빠르게 이해하고
선택하는 지도 역할을 맡긴다.

| 캐릭터 | 역량 | 보여줄 증거 |
|---|---|---|
| Dorothy | Planning & Design | 공간 해석, 동선, 콘텐츠 구조 |
| ScareMuse | Immersive Media | 영상·빛·사운드가 공간에 적용된 장면 |
| RoarLink | Interactive Media | 관람객 행동과 반응 결과 |
| TinAI | AI Production & Solution | AI가 제작·운영·개인화에 기여한 실제 방식 |

캐릭터는 선택의 표지판이지 콘텐츠보다 먼저 읽히는 주인공이 아니다.

### 6-4. Capability + Evidence

네 역량을 각각 긴 타이틀 장면으로 반복하지 않는다. 각 항목은 다음 동일 구조를 쓴다.

1. 역량명
2. 고객/관람객의 문제
3. 파모즈가 한 일
4. 실제 프로젝트 이미지 또는 영상
5. 산출물·적용 기술·운영 범위
6. 관련 프로젝트 상세 진입

각 역량은 같은 정보 문법을 사용하되 미술 사건과 색조는 고유하게 유지한다.

### 6-5. Selected Works

프로젝트 한 장면의 우선순위:

1. 프로젝트 이미지/영상
2. 프로젝트명과 장소/연도
3. 파모즈 역할
4. 해결한 문제 또는 만든 경험
5. 상세 보기

기존의 진행 바·장식 선·작은 점은 제거하거나 매우 약화한다. 이미지가 전체 화면을
채우더라도 상·하단 음영으로 텍스트 가독성을 보호하고, 모바일/태블릿에서는
`object-position` 또는 focal-point 데이터로 핵심 피사체를 보존한다.

### 6-6. Process / Team / Contact

- 기획 → 설계 → 제작 → 설치/운영의 실제 범위
- 팀 또는 협업 방식
- 회사명·연락처·소재지 등 확인된 정보
- 문의/프로젝트 상담으로 가는 명확한 CTA

마지막도 파티클 공간을 완전히 끄지 않되 낮은 비용의 안정 상태로 둔다.

---

## 7. 내비게이션과 인터랙션 문법

### 7-1. 고정 메뉴

첫 진입 이후 상시 접근 가능한 일반 메뉴를 제공한다.

```text
FAMOZ | About | Capabilities | Works | Process | Contact
```

- 데스크톱: 상단 고정, 배경과 분리되는 낮은 농도의 표면
- 모바일: 햄버거 또는 간결한 상단 메뉴; 기존 하단 챕터 레일은 제거 또는 보조화
- 현재 섹션에 `aria-current` 적용
- 해시 직접 진입과 뒤로가기 지원
- 메뉴가 WebGL 위에서 사라지거나 암부에 묻히지 않게 한다.

### 7-2. 하나의 인터랙션 언어

| 입력 | 항상 같은 의미 |
|---|---|
| 스크롤 | 다음 정보로 이동하고 공간 시점이 연속적으로 변함 |
| 포인터 이동 | 배경 공간이 낮은 강도로 반응; 필수 정보나 페이지를 바꾸지 않음 |
| 클릭/탭 | 사용자가 명시적으로 선택한 상세·프로젝트·메뉴를 엶 |
| 자동 진행 | Intro/쇼릴의 보조 장치; 사용자 입력 즉시 우선 |

금지:

- 같은 제스처가 어떤 장면에서는 페이지 전환, 다른 장면에서는 장식 반응이 되는 구조
- 임계점에서 모든 레이어가 동시에 갑자기 교체되는 전환
- 사용자가 원인을 알 수 없는 자동 점프
- 포인터 hover만으로 핵심 정보를 숨기거나 교체

### 7-3. 자동 진행

“스크롤하지 않아도 일부가 보인다”는 요구는 전체 사이트 자동 스크롤로 해결하지 않는다.

- Hero의 짧은 쇼릴/인트로만 무입력 상태에서 천천히 진행 가능
- 사용자 스크롤·포인터·키 입력이 들어오면 즉시 제어권을 사용자에게 넘김
- 페이지 자체는 자동으로 다음 섹션으로 납치하지 않음
- 재방문 사용자에게 Intro 반복을 강요하지 않음
- `prefers-reduced-motion`에서는 자동 움직임을 정지 이미지/짧은 페이드로 대체

---

## 8. 시각 위계와 톤앤매너

### 공통 위계

1. 현재 섹션의 핵심 정보
2. 이를 증명하는 프로젝트/결과 비주얼
3. 설명과 메타데이터
4. 캐릭터·파티클·장식

사람 얼굴과 캐릭터는 본능적으로 강한 시선을 받으므로, 본문 배경으로 쓸 때는
명도·대비·크롭을 낮추고, 증거 비주얼로 쓸 때만 명확하게 노출한다.

### 타이포 체계

- Hero display, section title, project title, body, metadata의 5단계 이내
- 장면마다 임의로 새로운 크기 체계를 만들지 않음
- 한글 대형 제목은 2–3줄 이내, 본문과 명확한 차이
- 대형 제목이 배경 경험과 증거 이미지를 모두 가리지 않도록 폭 제한

### 장식 정리

- 기능 없는 bar, line, dot, scan marker는 제거
- 남기는 선은 구획, 진행, 선택 중 하나의 명확한 의미가 있어야 함
- 같은 의미의 진행 UI를 섹션마다 다르게 만들지 않음
- 배경 변화가 본문보다 먼저 튀어나오면 대비와 움직임을 낮춤

미술 규칙은 `.claude/skills/famoz-art-direction/SKILL.md`를 그대로 따른다.
이번 IA 작업을 핑계로 모든 장면을 블루·바이올렛으로 통일하지 않는다.

---

## 9. 반응형 기준

모바일은 데스크톱의 좌우 구도를 축소하지 않고 별도 편집한다.

- 첫 화면 안에 회사 정의·대표 시각·주 CTA 중 최소 2개가 보일 것
- 캐릭터 얼굴과 핵심 도구에 focal point를 정의할 것
- `object-position`을 프로젝트/캐릭터 데이터로 관리할 것
- 캐릭터와 텍스트가 겹치면 상하 구조로 전환할 것
- sticky 체류 시간을 데스크톱보다 줄일 것
- 하단 브라우저 UI와 safe-area를 고려할 것
- 가로 390px 기준 가로 overflow 0
- 844×390 가로 모바일에서도 메뉴·제목·CTA가 잘리지 않을 것

검증 뷰포트:

- 1920×1080
- 1440×900
- 1280×720
- 834×1194
- 390×844
- 844×390

---

## 10. 구현 순서

### Phase A — 구조 측정과 콘텐츠 인벤토리

1. 현재 전체 스크롤 길이와 각 섹션 높이를 기록한다.
2. 모든 문장·프로젝트·회사 정보·CTA를 목록화한다.
3. 중복 선언, 근거 없는 문장, 잘못되거나 비어 있는 필드를 표시한다.
4. 현재 WebGL/스크롤/내비게이션 의존 관계를 도식화한다.

**산출물:** 수정 전 IA 표와 삭제/통합/유지 목록.  
이 단계 없이 바로 컴포넌트를 삭제하지 않는다.

### Phase B — 일반 내비게이션과 새 페이지 골격

1. `About / Capabilities / Works / Process / Contact` 앵커 확정
2. 고정 상단 메뉴 구현
3. 새 섹션 골격을 기존 컴포넌트와 병행해 구현
4. 키보드, hash, 뒤로가기, 모바일 메뉴 검증

### Phase C — 중복 구간 통합

1. `OzPortalHub`를 Capability Map으로 축소
2. `MascotScene + WhatWeCreate + ValueScene + PublicValue`의 중복 메시지를
   Capability + Evidence 구조로 통합
3. 각 선언에 실제 Works 데이터 또는 확인된 제작 범위를 연결
4. 정보가 보존된 것을 확인한 뒤 예전 중복 컴포넌트를 페이지 조립에서 제외

### Phase D — Works와 모바일

1. Works의 장식성 진행 UI 정리
2. 프로젝트별 역할·문제·결과 정보 추가
3. 반응형 focal point 및 모바일 상하 구도 적용
4. 인물/캐릭터 머리·표정·핵심 오브젝트 크롭 검수

### Phase E — 움직임 통일

1. 장면 경계의 즉시 교체 제거
2. 배경 → 증거 비주얼 → 제목 → 상세 순서로 진입 리듬 통일
3. 포인터는 공간 반응에만 사용
4. 자동 진행은 Intro에만 제한
5. 기존 파티클 성능을 회귀시키지 않았는지 실제 GPU에서 확인

---

## 11. 개발 금지 사항

- main/master 브랜치 직접 커밋
- 새 WebGL canvas 또는 장면별 renderer 생성
- Hero 프리셋을 다른 장면에 복사
- `scrollBus` 외 전역 스크롤 리스너 추가
- `pointermove`에서 React state 또는 무거운 계산 실행
- IA 개선을 이유로 프로젝트 데이터·회사 정보 삭제
- 실제 근거 없는 수치·고객명·성과 작성
- 실제 자산이 없는데 생성형 임시 이미지를 실적으로 표시
- 모바일을 데스크톱 CSS 축소만으로 완료 처리
- build 통과만으로 시각 개선 판정
- SwiftShader FPS를 성능 근거로 사용

---

## 12. 검증 게이트

각 게이트를 통과하기 전 다음 단계 완료를 선언하지 않는다.

### Gate 1 — 5초 이해

- 첫 5초 안에 “공간 경험을 기획·제작하는 FAMOZ”가 이해되는가?
- 첫 2개 화면 안에 실제 프로젝트 증거가 보이는가?
- 사용자가 메뉴를 발견하고 Works/Contact로 즉시 갈 수 있는가?

### Gate 2 — 구조

- “문 다음에 또 문”이 사라졌는가?
- 같은 선언이 캐릭터·What·Value에서 반복되지 않는가?
- 모든 긴 sticky 구간에 화면별 신규 정보가 있는가?
- 각 섹션의 목적과 다음 행동이 명확한가?

### Gate 3 — 인터랙션

- 스크롤, 포인터, 클릭의 의미가 전 구간에서 일관적인가?
- 임계점에서 화면이 갑자기 훅 바뀌지 않는가?
- 배경과 캐릭터가 본문 읽기를 방해하지 않는가?

### Gate 4 — 증거

- 관람객 중심·몰입·AI 주장마다 사례 또는 수행 방식이 붙는가?
- Works에서 프로젝트명, 역할, 만든 경험이 이해되는가?
- 파모즈가 무엇을 해주는 회사인지 텍스트만 믿지 않아도 되는가?

### Gate 5 — 모바일

- 얼굴·머리·핵심 도구가 의도 없이 잘리지 않는가?
- 제목과 이미지가 서로 가리지 않는가?
- 메뉴와 CTA가 safe-area 안에서 작동하는가?
- 빈 스크롤 체류 구간이 없는가?

### Gate 6 — 기술 회귀

- `npm run build` 통과
- WebGL 환경과 `--disable-3d-apis --disable-gpu` 환경 모두 콘텐츠 접근 가능
- `prefers-reduced-motion` 정상
- 콘솔 오류와 실패 요청 0
- 기존 파티클 공간이 마지막 섹션까지 유지
- 실제 GPU에서 포인터 이동 버벅임 재발 없음

---

## 13. 결과 보고 형식

완료 보고에는 아래를 반드시 포함한다.

1. 수정 전후 전체 IA와 총 스크롤 길이
2. 삭제·통합·보존한 컴포넌트 목록과 이유
3. 첫 5초/첫 2화면에 노출되는 회사 정의와 증거
4. 역량별 연결 프로젝트 또는 수행 근거
5. 내비게이션·hash·키보드 동작 결과
6. 데스크톱 3종, 태블릿, 모바일 세로/가로 스크린샷
7. WebGL 및 no-WebGL 결과
8. 실제 GPU 포인터 테스트 결과
9. 아직 근거 자료가 없어 임시로 남긴 콘텐츠
10. 통과하지 못한 검증 게이트

코드 변경량을 성과로 보고하지 않는다. 최종 브라우저에서 처음 보는 사람이
파모즈의 정체성, 역량, 실적, 다음 행동을 빠르게 이해하고, 경험 장치가 그 이해를
방해하지 않을 때만 완료로 판정한다.

---

## 14. Claude의 첫 응답 요구사항

코드를 수정하기 전에 먼저 아래 네 항목만 보고한다.

1. 현재 구조에서 통합할 중복 콘텐츠 목록
2. 새 IA에 매핑할 기존 컴포넌트 표
3. 데이터가 부족해 확정할 수 없는 프로젝트/회사 정보
4. Phase A에서 측정할 항목과 예상 변경 파일

이 사전 보고가 승인되기 전에는 파티클·색상·Bloom·카메라 또는 영상 효과를
변경하지 않는다.
